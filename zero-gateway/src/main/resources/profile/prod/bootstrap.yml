server:
  port: 6000
spring:
  application:
    name: Zero-Gateway
  cloud:
    nacos:
      discovery:
        server-addr: nacos.wogua-cloud:8848
        namespace: prod
      config:
        server-addr: nacos.wogua-cloud:8848
        # 配置文件格式
        namespace: pro
        file-extension: yml


